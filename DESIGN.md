# Design — BayJF 品牌规范

本文档记录 BayJF 品牌视觉的唯一权威定义。修改品牌资产前先读本文档。

## 品牌标：岸钩 JF（Shoreline Hook）

### 造型

一根竖笔立成岸：向右伸出的两道浪形横臂构成 **F**，笔尾向左弯出的潮钩
构成 **J**。字母即浪、浪即字母——海湾曲线与 BayJF 缩写互相成全。

```
竖笔 + J 钩:  M11.5 6.5 C 11 13, 11 19, 10 22.6 C 9.2 25.4, 5.6 26.2, 4.4 24
F 上臂（浪）: M11.3 9.5 C 16.5 7.3, 22.5 7.5, 27.3 9.8
F 中臂（浪）: M11.2 15 C 15.5 13.6, 20 13.8, 23.8 15.6  (opacity 0.55)
```

- 视窗：`viewBox="0 0 32 32"`，外框圆角 `rx="7"`
- 描边：主用 **3.4**（粗体版，favicon 与站内图标），圆头 `stroke-linecap="round"`
- 大尺寸展示（og、幻灯片封面）可用 **2.0** 细线版
- 高光：外框内叠加 `0.5px` 8% 白色内描边（暗夜家族统一质感手法）

### 主配色：海湾深蓝（Deep Bay）

| 角色 | 色值 |
|------|------|
| 底渐变起 | `#12354f` |
| 底渐变止 | `#08131d` |
| 图案渐变起 | `#7dd3fc` |
| 图案渐变中 | `#38bdf8` |
| 图案渐变止 | `#0284c7` |

底为对角线性渐变（左上→右下），图案渐变沿笔画走向（左上→右下）。
F 中臂（第三笔）固定 `opacity 0.55` 制造前后层次。

### 变体

| 变体 | 构成 | 用途 |
|------|------|------|
| 主版 | 海湾深蓝底 + 海沫蓝图案 | favicon、Header、Footer、og 卡片 |
| 纸白版 | `#f5f5f7` 底 + 深 sage 图案（`#54615b → #39433e`） | 浅色印刷、幻灯片、浅色上下文 |
| 细线版 | 主版配色 + 2.0 描边 | ≥96px 的展示场景 |

### 最小尺寸与净空

- 最小使用尺寸 **16px**（favicon 场景）；16px 下第三笔自然弱化属预期。
- 净空：图标四周保留 ≥ 图标宽度 25% 的留白。
- 不得：旋转、拉伸、改圆角比例、叠加阴影/描边、在图案上使用页面 sage 绿
  以外的其他彩色重着色。

## 字标（Wordmark）

- 写法固定为 `BayJF`（B、J、F 大写），系统字体栈 650–700 字重，
  字距 `letter-spacing: -0.025em`。
- 图标与字标间距约为图标宽度的 0.35 倍。
- 字标不单独重着色为渐变；深浅主题下跟随 `ink / paper` 文本色。

## 品牌家族

母品牌 BayJF（海湾蓝）与子产品落地页（agent-dev 青紫、pr-helper 粉紫、
tab-manager 绿等）共享同一图标语法：**暗夜渐变底 + 渐变图案 + 高光内描边**。
子品牌各自配色详见各仓库 `public/favicon.svg`。母品牌不再使用 sage 底
彩色图标，以避免与子品牌撞色、保持家族层级。

## 站内应用

| 位置 | 实现 |
|------|------|
| favicon | `public/favicon.svg`，`<link>` 带版本号（当前 `?v=3`），改图标必须递增 |
| Header | `src/components/Header.tsx`，26px `LogoMark` + BayJF 字标 |
| Footer | `src/components/Footer.tsx`，18px `LogoMark` 置于版权行前 |
| og 卡片 | `public/og.svg`（1200×630），左上 56px 图标 + `BAYJF · PORTFOLIO` |
| 共享组件 | `src/components/LogoMark.tsx`，`useId` 隔离渐变 ID，可多处共存 |

## 页面设计基线（既有，延续）

- 纯黑白页面：`paper #ffffff` / `night #000000`，卡片 `#f5f5f7` / `#1c1c1e`
- accent：sage `#54615b`（light）/ mint `#8fae9d`（dark）——仅用于页面
  交互元素，不再用于品牌标
- 纯系统字体栈，无 Web Font
- 动效克制：hover 位移/缩放在 200–300ms，`ease-out` 为主
